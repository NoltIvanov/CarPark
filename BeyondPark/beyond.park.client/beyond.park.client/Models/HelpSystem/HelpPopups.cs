namespace beyond.park.client.Models.HelpSystem {
    public enum HelpPopups {
        First,
        ClosestLocation,
        Filter,
        Reroute,
        MapPin,
        MoreTips,
        HideAll
    }
}
